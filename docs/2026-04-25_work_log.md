# 2026-04-25 Work Log

## 概要

本日の作業は、`docs/spec/total_todo.md` の P0-1 と P0-2 を中心に進めた。  
主目的は、client 側の `UserID` をクライアント生成 UUID に統一し、その `user_id` を `/api/account/plan` と `/api/voice-search/usage` で UUID 文字列として扱える状態にすることだった。

---

## 実施内容

### 1. P0-1 実装

- `IUserIdService` / `UserIdService` を追加
- `Preferences` に `user_id` を保存する実装を追加
- 初回未存在時に UUID を生成する実装を追加
- `SyncContext.UserId` を `string?` に変更
- `SplashPage` の `UserId = 1` 仮実装を撤去
- `MauiProgram` に `IUserIdService` を DI 登録

### 2. P0-1 動作確認用ログ追加

- `UserIdService` に `[UserID-Debug]` prefix 付きの `Debug.WriteLine` を追加
- `SplashPage` に `SyncContext.UserId` 設定確認用ログを追加

### 3. P0-1 動作確認結果

初回実行では以下を確認した。

- `Preferences` に `user_id` が未保存
- UUID を新規生成
- `SplashPage` で同じ値が `SyncContext.UserId` に設定

2 回目実行では以下を確認した。

- `Preferences` から同じ `user_id` を再取得
- 再生成されず、既存の UUID が継続利用された
- `SplashPage` でも同じ値が設定された

確認結果として、P0-1 は成立と判断した。

### 4. P0-2 実装

- `AccountPlanApiService` が `IUserIdService` から `user_id` を取得するよう変更
- `VoiceSearchUsageApiService` が `IUserIdService` から `user_id` を取得するよう変更
- `IVoiceSearchUsageApiService` の `RecordUsageAsync(int userId)` を `RecordUsageAsync()` に変更
- `VoiceSearchUsageRequestDto` を追加し、`user_id` を UUID 文字列で送るよう変更
- `VoiceSearchViewModel` から `int userId` を渡さない形へ変更

### 5. server 側確認

ユーザー報告ベースで、以下を確認済みと扱う。

- `server.js` 側も UUID 形式の `user_id` を受け取るよう変更済み
- サーバー側テーブル変更も実施済み
- 実機デバッグで、client 側生成の `int` ではない新しい `UserID` が server 側で更新されることを確認済み

### 6. 確認用ログの削除

- `[UserID-Debug]` ログ出力を削除

### 7. Todo 更新

- `docs/spec/total_todo.md` の P0-1 を完了化
- `docs/spec/total_todo.md` の P0-2 を完了化
- `docs/spec/total_todo.md` の P0-3 を完了化
- `docs/spec/total_todo.md` の P0-4 を完了化

### 8. P0-4 sync-context 依存の切り離し

- `AuthApiService.GetSyncContextAsync()` の実使用参照を確認
- 現行 Ver1.0 フローでは未使用であることを確認
- `MauiProgram` から `IAuthApiService` の DI 登録を削除
- `AuthApiService` / `IAuthApiService` / `SyncContextDto` は将来用コードとして残置
- `user_id` は引き続き `IUserIdService` を正とし、`/me/sync-context` に依存しない形を確認

### 9. P0-4 確認結果

以下を確認済み。

- `GetSyncContextAsync()` の未使用確認
- 起動
- プラン取得
- VoiceSearch の Free 利用
- Solo 切替
- Free への戻し

これにより、P0-1 から P0-4 までの現行 Ver1.0 フローは `IUserIdService` と account plan refresh ベースで成立していると判断した。

---

## 本日追加・変更した主なファイル

### 追加

- `ShoppingList002/ShoppingList002/Services/IUserIdService.cs`
- `ShoppingList002/ShoppingList002/Services/UserIdService.cs`
- `ShoppingList002/ShoppingList002/Models/Dto/VoiceSearchUsageRequestDto.cs`
- `docs/spec/total_todo.md`

### 変更

- `ShoppingList002/ShoppingList002/MauiProgram.cs`
- `ShoppingList002/ShoppingList002/Models/Sync/SyncContext.cs`
- `ShoppingList002/ShoppingList002/Models/Dto/SyncContextDto.cs`
- `ShoppingList002/ShoppingList002/Views/SplashPage.xaml.cs`
- `ShoppingList002/ShoppingList002/Services/Sync/AccountPlanApiService.cs`
- `ShoppingList002/ShoppingList002/Services/Sync/IVoiceSearchUsageApiService.cs`
- `ShoppingList002/ShoppingList002/Services/Sync/VoiceSearchUsageApiService.cs`
- `ShoppingList002/ShoppingList002/ViewModels/VoiceSearchViewModel.cs`
- `ShoppingList002/ShoppingList002/ViewModels/CandidateListPageViewModel.cs`
- `docs/spec/total_todo.md`

---

## ビルド状況

`dotnet build ShoppingList002.sln` は引き続き失敗した。  
ただし、残っている主エラーは今回の UUID 対応ではなく、Windows ターゲットで Android 名前空間を直接参照している既存コードが原因だった。

主な残エラー対象:

- `Services/AndroidKanaService/AndroidUserDictService.cs`
- `Services/AndroidKanaService/UserDictService.cs`
- `Services/UserDictService.cs`
- `ViewModels/Base/BaseVoiceAddViewModel.cs`
- `ViewModels/ShoppingListPageViewModel.cs`

---

## 現在の進捗

- P0-1: 完了
- P0-2: 完了
- P0-3: 完了
- P0-4: 完了
- P1-1: 完了
- restriction notice / help metadata safety rule: 完了

---

## 次の候補

- `docs/spec/total_todo.md` の P0-3
- ただしその前に、Windows ビルドを阻害している Android 依存コード混在の整理が必要になる可能性が高い
